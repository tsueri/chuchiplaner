services:
  app:
    build: .
    restart: unless-stopped
    ports:
      - "8000:8000"
    volumes:
      - ./data:/app/data
    env_file: .env

  backup:
    build: ./backup
    restart: unless-stopped
    volumes:
      - ./data:/data:ro
      - ./backups:/backups
    env_file: .env
